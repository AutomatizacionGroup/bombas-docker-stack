version: "3"
services:
    nodered: 
        image: nodered/node-red
        ports:
            - 1880:1880
        volumes:
            - nodered-data:/data
        networks:
            - frontend
        restart_policy:
            condition: on-failure

networks:
  frontend:

volumes:
  nodered-data:
